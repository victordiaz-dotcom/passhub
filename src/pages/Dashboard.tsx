// TODO: vista de administración — visitas del día, historial, gestión de
// usuarios/roles, gestión de colaboradores (reemplaza la hoja "DB SLACK"),
// y la generación de pre-registros + su QR (qrcode.react).
export default function Dashboard() {
  return <div className="p-6">TODO: Dashboard (Admin)</div>;
}
