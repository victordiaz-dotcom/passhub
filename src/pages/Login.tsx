// TODO: pantalla de login con supabase.auth.signInWithPassword().
// Recuerda: el registro público está cerrado (disable_signup) — las cuentas
// las crea el admin desde el panel (Edge Function create-user), igual que en AssetFlow.
export default function Login() {
  return <div className="p-6">TODO: Login</div>;
}
